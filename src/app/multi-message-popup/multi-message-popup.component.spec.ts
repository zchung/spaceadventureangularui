import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MultiMessagePopupComponent } from './multi-message-popup.component';

describe('MultiMessagePopupComponent', () => {
  // let component: MultiMessagePopupComponent;
  // let fixture: ComponentFixture<MultiMessagePopupComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ MultiMessagePopupComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(MultiMessagePopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
